This is Newsletter Studio, an offline-first PWA for authoring and printing vintage-newspaper-style
newsletters.

This app uses React (Vite) and ShadCN UI components.

Use `bun` and `bunx` instead of `node`, `npm`, and `npx`.
